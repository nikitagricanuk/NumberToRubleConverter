namespace NumberToRubleConverter;

class Program
{
    static void Main(string[] args)
    {
        int kopeks = int.Parse(Console.ReadLine());
        if (kopeks % 100 == 0)
        {
            
        }
        Console.WriteLine(kopeks);
    }
}